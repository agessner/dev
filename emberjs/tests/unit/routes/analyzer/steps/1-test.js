import { moduleFor, test } from 'ember-qunit';

moduleFor('route:analyzer/steps/1', 'Unit | Route | analyzer/steps/1');

test('Start fires transitionTo', function() {
  expect(1);

  // with moduleFor, the subject returns an instance of the route
  //var route = this.subject();
  
  

  //assert.ok(route.currentPath(), 'analyzer/steps/2');
});
